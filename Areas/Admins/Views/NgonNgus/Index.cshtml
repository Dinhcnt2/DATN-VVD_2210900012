@model IEnumerable<VVD_2210900012_DATN.Models.NgonNgu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenNgonNgu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenNgonNgu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaNgonNgu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaNgonNgu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaNgonNgu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
